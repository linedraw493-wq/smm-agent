---
type: log
status: active
sensitivity: normal
scope: work
updated: 2026-08-24
tags: [log, history, alya]
related: [status, HOME]
---

# Status-log — хронология по сессиям

Полная история того, что делалось и находилось, по сессиям. `status.md`
держит только текущее горячее состояние ([[status]]); всё, что стало
историей, переезжает сюда **целиком, без потерь** — вычеркивать и удалять
факты отсюда нельзя, дописывать новые сессии снизу можно.

Выделено из `status.md` 2026-08-24: файл вырос до 34 КБ построчным дневником
и перестал быть «горячим состоянием» по контракту
`meta-vault/domain-template.md` §Контракт status.md. Много из записанного
здесь уже целиком лежит в канонических местах — `projects/*/run.md`,
`trends/log/`, `school/` — эта хронология не заменяет их, а даёт быстрый
обзор по датам.

## Сделано 2026-08-23, вторая сессия

- Выпуск `projects/2026-08-23-mozg-agenta/` — девять текстов постов
  (4 Instagram, 5 Threads) по скриншотам работы агента: граф волта, страница
  «как достраивается мозг агента», воронка продаж школы «Лидер».
- **Тексты постов выведены из git** — слово владельца
  ([[decisions/2026-08-23-posty-ne-v-git]]). Правило зашито в `.gitignore`,
  операцию `napisat-post` и заготовку выпуска.
- Владелец обещал доступ к Threads и Instagram — когда появится, отменяется
  запрет из [[decisions/2026-08-23-alya-ruki-naruzhu]] и правятся обе двери.
- Ждём от владельца: узкое место воронки для дырки в тексте IG-2, называть ли
  школу прямо, публиковать скрины как есть или перерисовать.

## Что стоит и работает

- Папка, дверь, vault, операции, скиллы — есть.
- Стек монтажа поставлен и проверен на этой машине: ffmpeg 9.0 full-build
  (фильтры `ass`, `zscale`, `tonemap`, `loudnorm`, `arnndn`, `afftdn`,
  `sidechaincompress` на месте), Python 3.12, `faster-whisper`, `pysubs2`,
  `Pillow`, `yt-dlp`.
- Ремесло цеха перенесено в `craft/` с поправкой на Windows.
- Автономное обучение включено: задача `alya-trend-scan`, понедельники.
- Конвейер прогнан насквозь на тестовом клипе: субтитры, звук, сборка —
  все четыре гейта зелёные. На живом материале ещё не гонялся.
- Своё мнение записано — [[judgment]].
- **Шрифты поставлены** 2026-08-23: Inter (Regular/SemiBold/ExtraBold) и
  Playfair Display (SemiBold/SemiBoldItalic) нарезаны из вариативных в
  статические TTF, кириллица полная. Лежат в `assets/fonts/`, ffmpeg берёт их
  параметром `fontsdir` — **в систему ставить не потребовалось**.
- **Субтитры проверены рендером:** Inter, кириллица, плашка, контраст 9.5
  при пороге 6.6. Тест поймал две тихие ошибки формата ASS — обе исправлены
  и записаны в ремесло.
- **Палитра расфиксирована — их две.** Слово владельца: дизайн не выбран,
  его вариант чёрный с лаймом, у Нурса синий с белым. Цвет вынесен из кода
  в `presets/palette-*.json`, любой модуль работает с любой палитрой
  ([[craft/design-system]]). По умолчанию `ink-lime`.
- **Звук углублён.** `audio_report.py` — замер по шести полосам, ловит
  потолок 10 кГц (подпись мессенджера). `audio_polish.py` — эквализация,
  динамический де-эссер, пространство, стерео, громкость. Проверено на живом
  войсовере: присутствие +5.0 dB, воздух +5.6 dB, разборчивость +3.1 dB,
  −15.9 LUFS, пик −1.0 dB. Совпадает с целью канона (+4…+6 dB на 3–10 кГц).
- **Субтитры раскладываются по кадрам.** `subs_ass.py --scenes`: карта,
  перепрыгивающая склейку, режется по ней. Проверено — режет. Плюс акцент на
  ключевом слове, числа выделяются сами.
- **Выгружена на GitHub:** `linedraw493-wq/smm-agent`, приватный. Обновление
  через `tools/sync.ps1`, автономный прогон выгружает себя сам.
- **Инструменты разбора чужого готовы и проверены** на живом референсе:
  `tools/teardown.py` сошёлся с ручным разбором цеха до десятых (−14.5 LUFS,
  LRA 2.1). `tools/grid_test.py`, `tools/ref_get.ps1`.
- Модель хранения решена — [[decisions/2026-08-23-hranenie-i-segmentaciya]].
- Vault открыт в Obsidian: волт зарегистрирован на корень `reelsmaker/`.

## Проверка готовности 2026-08-23 — прогоном, не по записям

Полный путь пройден на **настоящем материале** (чужой референс, 20 с речи):
замер звука → обработка → распознавание → субтитры по кадрам → сборка →
гейты. Работает.

- Распознавание: 74 слова со словными таймингами, текст совпал с ручной
  расшифровкой. Модель скачана, **2.9 ГБ**, лежит вне git.
- Субтитры: 23 карты, 15 склеек найдено, 3 карты разрезано по кадрам,
  акцент проставлен.
- **Гейты проверены на браке**, а не только на хорошем файле: подсунутый
  кривой ролик (720p, моно, −36 LUFS, не та длина) завалил четыре гейта из
  четырёх, код возврата 1. Гейт, который всегда зелёный, — не гейт.

**Найдено и починено в ходе проверки:**

1. **Кроп уничтожал запись экрана.** Центральный кроп 16:9 в вертикаль
   сохранял 32% ширины — срезало оба края. А «экран-доказательство» — наш
   формат номер один. Добавлен выбор режима `--fit` с автоопределением
   ([[craft/video-station]]).
2. **Ложно-зелёный гейт.** На моно-дорожке проверка L/R печатала «OK» —
   сравнивать было не с чем. Теперь честное «не проверялись».

## Дополнено 2026-08-23, вторая половина дня

- **Рендерер плашек есть** — обложка, плашка, нижняя треть одним модулем.
  Проверено: контраст 18.7 при пороге 6.6.
- **Длинный материал** — `cuts.py`. Проверено на 40 минутах: 25 секунд,
  98× реального времени. Часы не страшны.
- **Найден предел ffmpeg:** выражение `select` не переваривает 193 куска.
  Выше 40 кусков сборка сама переходит на `trim+concat` — проверено, граф
  принимается.
- **Качество картинки** — профили `--look` и `--quality`, по умолчанию
  насыщенно и качественно. Замерено: насыщенность +31%, контраст +19%.
- **Кадрирование** — `--fit auto`: запись экрана вписывается, а не режется.
- **Музыка** — модуль сведения под голос с приседанием и реестр лицензий.
  Треков пока нет.
- **Шумодав** — шесть моделей RNNoise скачаны и проверены.
- **Голос бренда переведён в `active`** — владелец ответил на всё главное
  ([[brand]]).

## Школа заведена 2026-08-23, третья сессия — работа Кота

Слово владельца: «отправляй Алю учиться, в её нынешнем состоянии она не
сможет дать нормальный монтаж». Сделано с уровня владельца, в эту папку.
Решение — [[decisions/2026-08-23-shkola-ali]], карта — [[school/HOME]].

**Диагноз, а не догадка.** Разобран забракованный ролик
([[school/razbor-2026-08-23-reel-v3]]): контактный лист + замеры.

- Шесть событий за 22.5 с при норме 8–15. Средний план **4.50 с** при норме
  1.5–3, первая склейка **4.80 с** при норме ≤ 1.5. Нормы лежали в ремесле
  за сутки до сборки — и были нарушены втрое, потому что **их ничто не
  проверяло**.
- Первый кадр — асфальт. Хука нет. Последний — затухание в чёрное.
- Пересветы найдены и объяснены: сырьё **PQ**, а не HLG, а в цепочке стояло
  `npl=100` + `desat=0` — небо выжигалось в розовое **до** входа в грейд.
  Замер: пурпур в светах **+7.7 → +2.7**, выбито **0.36 % → 0.00 %**.

**Вывод одной строкой: знание было, приёмки не было.** Смотрел за Алю
владелец.

### Что теперь есть

- **Семь кафедр** с учебниками в `craft/`: [[craft/self-review]],
  [[craft/color]], [[craft/montage]], [[craft/sound-design]],
  [[craft/scriptwriting]], [[craft/marketing]], [[craft/platform-specs]].
- **Приёмка глазами обязательна** — [[operations/posmotret-rabotu]],
  рубрика из 12 вопросов, инструмент `tools/look.py` (контактный лист,
  кадр в полный размер, безопасная зона площадок).
- **Гейт ритма** `qa.py --rhythm`. Проверен на браке: забракованный ролик
  даёт четыре «БРАК» из четырёх там, где старые четыре гейта были зелёные.
- **Тонмап правлен:** `config.tonemap(npl)`, дефолт 400, `desat=2`.
  npl — параметр материала, подбирается свипом и пишется в `run.md`.
- **Профиль `rich` отменён**, дефолт `--look alive`: он нарушал два
  записанных правила разом (`eq=contrast` поверх кривой, `unsharp` после
  ужатия).
- **Громкость −16 → −14 LUFS.** Площадки нормализуют к −14, TikTok в ленте
  не нормализует вовсе; на −16 ролик звучал тише соседей.
- **Безопасная зона площадок** записана и рисуется: `look.py --safe`.
  Раньше её не было вовсе — текст мог уходить под интерфейс.

### Чего у школы нет

- **Корпус эталонов пуст.** Нужен список владельца: три-пять роликов,
  которые ему нравятся. Без него эталон соберётся по нашему вкусу.
- **Библиотеки SFX нет** — кафедра звука не закрывается.
- **Цифр после публикации нет** — кафедра маркетинга закрывается частично.
- **Ни один экзамен, кроме Э0, не сдан.** Учебники написаны, работой не
  подтверждены.

### Правка доказана пересборкой, а не заявлена

`out/reel-v4.mp4` — тот же материал, те же точки реза, менялась **только
картинка**: `npl=400`/`desat=2`, `--look alive`, `join_clips --match`,
`--xfade 0.5`.

- Небо держит деталь: выбито 0.36 % → ~0 %, пурпур в светах **+7.7 → +2.7**.
- Блоки сидят в одном тональном семействе — скачка на границах больше нет.
- **Гейт ритма по-прежнему красный, и это верно:** правилась картинка, а не
  монтаж. Первый кадр всё ещё асфальт, истории нет — это лечится сценарием.

Разбор захода — `projects/2026-08-23-m4ksi-reels-01/run.md`, заход 4.

### Гейт поймал сам себя

Кроссфейд прячет склейки от детектора сцен: ролик из семи кусков показался
ему одним планом. Гейт научен **отказываться судить** вместо того, чтобы
судить наугад (`--shots N`). Гейт, который врёт, хуже отсутствующего.

## Экзамены отменены, учёба — просмотром. 2026-08-23, вечер

Слово владельца: «мне экзамены ей как бы не нужны, эта модель сама должна
обучаться смотря instagram и shorts… а то она откровенно шляпу делает».
`school/exams.md` удалён, поправка — [[decisions/2026-08-23-shkola-ali]].

### Смотреть она теперь правда может

- **`yt-dlp` не был установлен вовсе**, хотя в этом файле числился рабочим.
  Её `ref_get.ps1` упал бы на первом же вызове. Поставлен.
- **`tools/watch.py`** — весь путь одной командой: канал → скачать → замерить
  → расшифровать крючок → контактный лист → карточка разбора.
- **YouTube Shorts открыт полностью** — список канала и скачивание без входа
  и без браузера. **Instagram закрыт**: профили `yt-dlp` не поддерживает,
  реелсы просят вход. Оттуда нужна прямая ссылка от владельца.

### Первый просмотр — и он сразу объяснил «шляпу»

Разбор — [[school/corpus/2026-08-23-kak-eto-vyglyadit]]. Замер по рилсам
блогеров ниши против нашего ролика:

| | они | мы (reel-v3) |
|---|---|---|
| средний план | **1.26–2.91 с** | **4.50 с** |
| планов на ролик | 10–32 | **5** |
| первая склейка | 0.85–2.67 с | **4.80 с** |
| LRA | 2.0–3.1 | в коде стояло **11** |
| длина | 29–56 с | 22.5 с |

**Главное, что увидела глазами:**

- **Сплит верх/низ** — экран сверху, говорящая голова снизу. У всех трёх
  авторов. Прямая замена нашей размытой подложки: та же площадь, но вместо
  размытой копии живое лицо. Раскладки в коде нет — самый дорогой хвост.
- **Ни одного выбитого окна ни у кого.** Свет из окна в каждом кадре и всегда
  с деталью. Ровно то, что мы завалили.
- **Цвет никто не крутит.** Дорого выглядит от света сбоку, дальнего фона и
  отсутствия цифровой резкости, а не от профиля. Три пункта из четырёх
  решаются на съёмке.
- **Событие ≠ рез камеры.** У Cole Medin говорящая голова снята одним планом
  на 80 секунд, меняется только верхняя половина. Ритм есть.

### Ещё сделано

- **Слой SFX больше не пустой** — `tools/sfx_make.py`, двенадцать звуков
  синтезом (вуши, райзеры, импакт, саб-дроп, клики, поп, ui-ok, свелл).
  **Права наши, лицензия не нужна, страйк невозможен.** Реестр заполнен.
  Это был приоритет №1 канона цеха, не внедрённый там ни разу.
- **`config.LRA` 11 → 5.** Вещательная норма против ленты. Замер референсов
  дал 2.0–3.1; более ранний разбор давал 2.1 — находили и не переносили.

### Чего нет

- **Instagram не посмотрен.** Нужны прямые ссылки от владельца.
- **Эталон вкуса владельца не назван** — смотрим тех, кто близок по теме.
- **Материал со звуком** обещан назавтра; до него кафедра звука разбирает
  чужое, но своего не проверяет. Текущий ролик остаётся немым — по слову
  владельца.

## Прогон «визуальный разбор» — 2026-08-23, ночь. Работа Али

Слово владельца: «Отправь Алю смотреть ютуб и инстаграм по ИИ, пускай
смотрит разных блогеров, обучается тому, как сложена их картинка, звук,
субтитры, как в целом выглядят их вставки и монтаж.»
Файл прогона — [[trends/log/2026-08-23-vizual-razbor]].

### Что сделано

- **Долг закрыт: 7 карточек**, где числа были сняты машиной, а раздел «Что
  видно глазами» пуст. Разбора там не было — теперь есть.
- **Разобрано 10 новых роликов**, шесть авторов, два из них новых —
  **Riley Brown** и **Cursor**. Итого **17 карточек заполнено глазами** за
  прогон, корпус вырос до **24 роликов**
  ([[school/corpus/2026-08-23-koridor]]).
- **Instagram посмотрен.** Сначала гостем, потом — после того как владелец
  вошёл в аккаунт сам — полностью.
- **Девять общих приёмов сведены в одну сторону** — раздел в конце файла
  прогона. Один из них измерен числами, остальные видны глазами.

### Что изменилось в понимании

- **Ритм делает не рез камеры.** У трёх роликов детектор нашёл ноль
  склеек — и был прав: нижняя половина снята одним дублем на 90–170 секунд,
  а ритм создаёт **смена верхней панели**. Значит **гейт ритма меряет не
  то**: он считает склейки камеры. Хвост в [[backlog]].
- **Формат «ролик без единой съёмки» работает.** 90 секунд типографики у
  Cole Medin; вся лента Cursor в Instagram — только интерфейс, ни одного
  лица. Это снимает нашу главную нехватку и даёт готовый продукт сегодня.
- **Обложка решает больше монтажа, и под этим есть числа.** Плашка-заголовок
  на обложке — 59 тыс. — 1 млн просмотров; без заголовка — 7–18 тыс.
- **Наши −14 LUFS и LRA 5 подтверждены замером** по 24 роликам: средняя
  громкость чужих −22.7, в цель попал ровно один. Это наше преимущество,
  менять не надо.
- **В нашей нише Instagram блогерами брошен**: Nate Herk — 950 тыс. на
  YouTube против 7 тыс. в Instagram; у n8n сетка пустая. Всерьёз ведёт один
  Riley Brown.

### Права расширены — два решения владельца в этот день

- **[[decisions/2026-08-23-alya-polnyy-dostup]]** — «полный доступ как
  самостоятельной модели кроме допуска к постингу (временно)». Правило
  «ход первый — разговор» с Али снято по всему ремеслу; постинг наружу
  закрыт и снимается **отдельной фразой владельца**.
- **[[decisions/2026-08-23-alya-ruki-naruzhu]], поправка** — чтение через
  Chrome разрешено: «залезай в инст в хроме если надо». Публикация,
  подписка, лайк, ответ — по-прежнему нет. Пароли, коды, капчи — не её руки.

Дверь `../CLAUDE.md` правлена под оба решения.

### Починено по ходу

- **`watch.py` больше не стирает разбор.** Повторный прогон переписывал
  карточку начисто; следующий понедельник стёр бы всю эту работу. Теперь
  карточка со словами «Разобрано глазами» пропускается.
- **Опознаны авторы «NA»** в четырёх старых карточках: три — Nate Herk,
  одна — Liam Ottley. **Найден дубль:** один ролик скачан дважды в две
  папки (md5 совпал).
- **Ручка `@rileybrown` неверна, верная `@rileybrownai`** — в watchlist
  исправлено. У `@cursor_ai` вкладки Shorts нет вовсе, только горизонталь.

### Чего по-прежнему нет

- **Ни одного своего прогона по этим находкам.** Все девять приёмов живут
  только в `trends/` — в `craft/` не ушло ничего, и не уйдёт, пока не
  сработает на нашем ролике. Это условие права на автономию.
- **`doctor.ps1` врёт про стек** — печатает «БРАК ffmpeg/ffprobe» на
  исправной машине, потому что смотрит в `PATH`, а не в `config.py`.
- **Три ручки вендоров не подтверждены** (Make.com, Mikey No-Code,
  LM Studio) — нужна ссылка, наугад не подбираем.
- **Раскладки «сплит верх/низ» в коде так и нет** — самый дорогой хвост,
  теперь подтверждённый девятью роликами из двадцати четырёх.

## Прогон «тест тёплого кадра» — 2026-08-24. Работа Али

Выпуск `projects/2026-08-24-test-tyoplyy-kadr/`, замер целиком — в его
`run.md`. Наружу не идёт: это проба взгляда, не ролик.

**Что это.** Тот же материал, что у первого выпуска, **пересобран по
монтажу** (13 планов вместо 7, кроссфейд 0.15 вместо 0.5, три панч-ина,
первый кадр с человеком, финал кадром) и прогнан через **две цепочки
грейда** — `out/warm-a.mp4` и `out/warm-b.mp4`, 22.87 с обе. Владелец
выбирает взгляд, а не пересматривает два ролика.

### Гейт ритма больше не красный

Экзамен Э2 в части ритма закрыт: средний план 1.90 с, самый длинный 2.70 с,
планов 13 при норме ≥ 8. Первый кадр — рука со стаканом кофе, финал — рука
в камеру, `--fade` не ставился. Первая склейка **2.05 с при норме 2.0** —
единственная мерка ритма мимо, и мимо на 0.05 с.

**Сценария по-прежнему нет.** `script.md` выпуска — шаблон. Три вопроса
рубрики из блока «Смысл» (хук, один вопрос, призыв) отвечены «нет» именно
поэтому. Э2 в части сценария **не закрыт**.

### Цвет: попали в референс, и это измерено

`grade_match.py` против фотографии владельца `08`, по каждой версии
отдельно — печатается правка, которой **ещё не хватает**:

| | синий в светах | контраст | насыщенность |
|---|---|---|---|
| v4 | −0.091 | ×1.147 | ×0.947 |
| warm-a | −0.027 | ×1.076 | ×1.457 |
| **warm-b** | **+0.003** | **×1.044** | ×1.359 |

Синева в светах у `warm-b` погашена в ноль. Насыщенность мимо у обеих: мы
на 27–32 % бледнее фотографии, и это осознанный шаг, но не попадание.

**Моё мнение: `warm-b`.** Ближе по трём меркам из четырёх; её единственный
перелёт (тепло в светах +36 против +27.8 у референса) правится ослаблением
одной кривой; недобор `warm-a` в средних — только новой цепочкой.

### «Небо выбито на дорожных кадрах» — проверено, оба утверждения неверны

Кот забраковал дорожные планы словами «небо плоское, выбито в белое, грейд
его не тронул». Стенд `work/sky.py`:

- **выбито** — доля неба ≥ 250 составляет 0.00–1.16 %. Не выбито.
- **не тронул** — тепло в небе +1.07 → +34.31. Тронул, и сильно.
- **виновато другое:** небо слишком **яркое** (226–234 luma, до 67 % выше
  240), а не белое и не холодное. Пятно почти-белого на треть кадра глаз
  читает как пересвет любого оттенка.
- **потолок исходника — наполовину:** на клипе `03` размах неба в сыром PQ
  23 кода из 255, облаков там нет вовсе. На клипе `04` — 36, материал ни
  при чём.
- **чинится тонмапом, не цветом:** `npl=700` роняет долю ≥ 240 с 44.6 % до
  0.0 % и поднимает контраст внутри неба на 25 %.

Не починено в этом заходе намеренно: `join_clips --npl` один на весь ролик,
а `npl=700` убивает кадр у столика (яркость 176.8 → 135.6) — тот самый, что
единственный попал в референс. Покусковый npl — хвост в [[backlog]].

### Гейт глазами пройден полностью

Рубрика из 12 вопросов — в `run.md` выпуска. **6 «да», 4 «нет», 1 «да с
оговоркой», 1 не применимо.** Три «нет» из четырёх — блок «Смысл», и все
три от отсутствия сценария. Четвёртое — пункт 8, сведение планов: дорожный
план стоит на яркости 94–95 при 63–74 у соседей, разброс по ролику 31.8
против 25.3 у v4, то есть **хуже, чем у v4**. Это моё «нет», не чужое.

### Ремесло дополнено — четыре дельты, все куплены замером

- [[craft/color]] — **плоское небо ≠ выбитое небо**: диагноз по СКО и
  размаху p1–p99 внутри маски неба, три разные причины и что лечит каждую.
- [[craft/color]] — **пурпур читается в обе стороны**: порог был
  односторонним, а олива уводит в минус до −15 при референсе −24.8.
- [[craft/color]] — **цель по зонам снимается замером референса**, а не
  прикидкой: в стенде стояло +4..+10 и +10..+22, замер фотографии дал
  +21.5 и +27.8.
- [[craft/montage]] — **`qa.py --shots N` печатает не замер, а деление**:
  ровные числа по всем четырём меркам ритма и есть признак подлога.

### Чего не хватило

- **Музыки нет** — вопрос владельцу лежит в `inbox/`, ролики немые.
- **Сценария нет** — и это, а не цвет, теперь главный тормоз.
- **e11/e12 вялые** — два плана подряд одной крупности с одним фоном в
  самом конце ролика, там, где зритель решает досматривать.
