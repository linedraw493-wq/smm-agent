# log — сырьё разведки

По файлу на прогон: `ГГГГ-ММ-ДД.md`. Что нашли, дословно и со ссылкой.
Выводы здесь не делаются — они в `../digest.md`, и только после проверки
прогоном.

Отрицательный результат («попробовали, не сработало») **не стирается**.
