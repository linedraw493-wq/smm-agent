---
type: status
status: active
sensitivity: normal
scope: work
updated: 2026-08-23
tags: [status, hot-state]
related: [HOME, backlog, questions-vladelcu]
---

# Состояние на 2026-08-23

Аля **заведена сегодня**. Живых прогонов ещё не было — ни одного ролика.

## Что стоит и работает

- Папка, дверь, vault, операции, скиллы — есть.
- Стек монтажа поставлен и проверен на этой машине: ffmpeg 9.0 full-build
  (фильтры `ass`, `zscale`, `tonemap`, `loudnorm`, `arnndn`, `afftdn`,
  `sidechaincompress` на месте), Python 3.12, `faster-whisper`, `pysubs2`,
  `Pillow`, `yt-dlp`.
- Ремесло цеха перенесено в `craft/` с поправкой на Windows.
- Автономное обучение включено: задача `alya-trend-scan`, понедельники.
- Конвейер прогнан насквозь на тестовом клипе: субтитры, звук, сборка —
  все четыре гейта зелёные. На живом материале ещё не гонялся.
- Своё мнение записано — [[judgment]].
- **Шрифты поставлены** 2026-08-23: Inter (Regular/SemiBold/ExtraBold) и
  Playfair Display (SemiBold/SemiBoldItalic) нарезаны из вариативных в
  статические TTF, кириллица полная. Лежат в `assets/fonts/`, ffmpeg берёт их
  параметром `fontsdir` — **в систему ставить не потребовалось**.
- **Субтитры проверены рендером:** Inter, кириллица, плашка, контраст 9.5
  при пороге 6.6. Тест поймал две тихие ошибки формата ASS — обе исправлены
  и записаны в ремесло.
- **Палитра исправлена на фирменную.** Замер макетов `maksi/social-mockups/`
  показал: стиль M4ksi — **чёрный `#000000` + лайм `#d9fa03` + кремовый**.
  Графит с синим, перенесённый из канона видео-цеха, — личный бренд
  Скандара, не компания. Переключено; ждёт подтверждения владельца
  ([[questions-vladelcu]] п.8).
- **Инструменты разбора чужого готовы и проверены** на живом референсе:
  `tools/teardown.py` сошёлся с ручным разбором цеха до десятых (−14.5 LUFS,
  LRA 2.1). `tools/grid_test.py`, `tools/ref_get.ps1`.
- Модель хранения решена — [[decisions/2026-08-23-hranenie-i-segmentaciya]].
- Vault открыт в Obsidian: волт зарегистрирован на корень `reelsmaker/`.

## Чего нет

- **Ни одного прогона.** Все операции в статусе `in-dev`: `live` ставится
  только после живого случая.
- **Разбора чужих роликов нет.** Разведка смотрит, *что* вышло, но не
  *как сделано*. Это ступень 3 в [[roadmap]] и следующая работа.
- **Модель распознавания не скачана** — подтянется при первом запуске
  `tools/transcribe.py` (около 1.5 ГБ, в git не едет).
- **Голос бренда — черновик.** [[brand]] в статусе `proposed`.
- **Площадка и язык роликов не названы.** См. [[questions-vladelcu]].
- **Рук наружу нет.** Работа в браузере владельца предложена и остановлена им
  же 2026-08-23 ([[decisions/2026-08-23-alya-ruki-naruzhu|запись]]). Публикует
  человек; цифры после публикации снимать некому.
- Git в папке не заведён — ждёт слова владельца.

## Условлено с владельцем

- **Первый монтаж — сегодня, 2026-08-23**, в течение пары часов; обсуждение
  за полчаса до. Владелец даёт материал, Аля собирает. Это ступень 4
  в [[roadmap]].
- **Выбор катов** (что выкинуть из сырья) — обсуждается отдельно, решение
  ещё не принято. Это ступень, без которой монтаж длинного материала не
  автоматизируется.

## Ближайший шаг

Первый выпуск. Он же переведёт операции из `in-dev` в `live` и даст первую
строку в `trends/digest`.
