---
type: reference
status: active
sensitivity: normal
scope: work
updated: 2026-08-23
tags: [craft, subtitles, ass, whisper, timing]
related: [video-station, design-system, ../HOME]
---

# Ремесло — субтитры

## Форма

- **Карты по 2–4 слова**, вербатим. Не пересказ, не литературная правка:
  зритель читает то же, что слышит.
- Тайминги — из **word-timestamps** распознавания, не на глаз.
- Стиль — не обводка, а **плашка**: `BorderStyle=3`, графитовая подложка.
  Обводка на пёстром фоне плывёт, плашка держит текст всегда.
- Файл — **ASS**, не SRT. ASS несёт оформление внутри себя: кегль, плашку,
  позицию, подсветку. Вшивается фильтром `ass=`.

## Три языка, и один из них проблемный

Слово владельца 2026-08-23: русский основной, английский иногда, казахский
тоже.

⚠️ **Казахский модель распознаёт заметно хуже** русского и английского. Она
его знает, но ошибается чаще. Правило: казахская речь распознаётся, а потом
**вычитывается глазами** до сборки субтитров. Не вычитала — не собирай.
Ошибки в субтитрах на родном языке зрителя дороже, чем их отсутствие.

Смешанная речь (русский с английскими терминами) распознаётся нормально, но
термины проверять: модель любит переводить их на слух.

## Раскладка по кадрам, а не только по времени

Слово владельца 2026-08-23: субтитры надо распределять покадрово, понимая,
что происходит в видео. Это не украшение — это про удержание.

**Карта не должна перепрыгивать склейку.** В момент реза глаз уходит на
новую картинку и теряет строку: зритель начал читать на одном кадре, а
дочитывает на другом — и не дочитывает. Поэтому карта, попавшая на склейку,
режется по ней: `subs_ass.py --scenes work/clip.mp4`.

Порог обнаружения склейки — 0.30, тот же, которым мы меряем чужие ролики в
`tools/teardown.py`. Один порог на своё и чужое: иначе сравнение нечестное.

Осколок короче **0.35 с** не плодим — мигание строки хуже, чем строка
внахлёст. Это граница, а не догма: на очень частом монтаже её поднимают.

⚠️ Порог видит **жёсткие** каты. Плавные переходы он пропускает — и это не
баг: в нашем жанре кроссфейдов нет ([[video-station]]).

## Акцент — одно слово, не два

`--accent "слово,слово"` красит названные слова цветом акцента текущей
палитры. **Числа красятся сами**: в нашем ремесле число в кадре — то, что
должно остаться в памяти, и оно же — половина силы крючка.

Два акцента в кадре — это уже не акцент. Если хочется выделить всё, значит
карта написана плохо: её надо переписать, а не раскрасить.

## Тайминг после монтажа

Если ролик резался и ускорялся, сырые тайминги врут. Карта катов и ускорений
пересчитывает их в выходную шкалу (`out_t()`).

**CUTS — единственный источник правды.** Скрипт сборки и генератор субтитров
обязаны читать одну и ту же карту катов. Разошлись — субтитры уедут, а
ошибка тихая: ничего не падает.

Сырой клип без монтажа — тайминги идут прямо из word-timestamps, без пересчёта.

## Распознавание на этой машине

`faster-whisper`, модель `large-v3` (русский держит), выход — `words.json` со
словными таймингами. Модуль — `tools/transcribe.py`.

Модель **скачана 2026-08-23**, лежит в `tools/models/` вне git и занимает
**2.9 ГБ** — не 1.5, как было записано сначала. Первый прогон на новой
машине качает её заново и это не быстро: скачивать заранее, а не за десять
минут до монтажа.

Проверено на живой речи: 20 секунд, 74 слова со словными таймингами, текст
совпал с ручной расшифровкой.

⚠️ **Whisper врёт на первых словах**, особенно с `-ml 1`: кладёт речь в
тишину. Начало резать по **энергии сигнала**, а не по его таймингу.

## Две ловушки формата ASS

Обе пойманы 2026-08-23 на первой сборке, обе тихие — ничего не падает.

1. **До текста ровно 8 полей**, не девять: `Layer, Start, End, Style, Name,
   MarginL, MarginR, Effect`. Лишняя запятая не ломает файл — она просто
   уезжает в начало текста, и в кадре появляется `,Менеджер`.
2. **При `BorderStyle=3` плашку рисует `OutlineColour`, а не `BackColour`.**
   `BackColour` отвечает за тень. И цвет пишется **задом наперёд**:
   `&HAABBGGRR`. Лайм `#d9fa03` кодируется как `...03FAD9`, а не `...D9FA03`.
   Напишешь в привычном порядке — получишь синий вместо лаймового, и на глаз
   это заметно не сразу. (Чёрная плашка этой ловушки не показывает: у чёрного
   все три байта нулевые — тем опаснее, ошибка всплывёт на первом же цветном
   элементе.)

Оба правила зашиты в `presets/subs-m4ksi.ass` комментарием — чтобы тот, кто
полезет править стиль, прочитал их до, а не после.

## Шрифты — в системе не нужны

Шрифты лежат в `assets/fonts/`, ffmpeg получает их параметром
`fontsdir` фильтра `ass`. Ставить в Windows ничего не надо: реестр не
трогаем, а сборка остаётся переносимой — папку скопировали, шрифты уехали
с ней.

## Позиция в кадре

Держать **вне зоны интерфейса площадки**: низ занят подписью и кнопками,
верх — ником. Безопасная зона — центр по вертикали, ближе к нижней трети,
с запасом ≥ 15% высоты кадра снизу.

Грид площадки режет превью примерно до 3:4 — всё, что должно читаться в
сетке, класть в **центральную грид-безопасную зону**.

## Гейт

Контраст текст↔плашка меряется на **ярчайшем кадре окна**, порог WCAG **6.6**.
Не прошёл — поднять непрозрачность подложки до сборки. Подробности —
[[video-station]] §Четыре гейта.
