---
тип: операция
действие: собрать-рилс
вызов: фраза · скилл /reel
исполнитель: код → tools/build_reel.py
статус: in-dev
---
# Операция — собрать рилс

**Когда:** «собери ролик» · «смонтируй из того, что в raw» · «пересобери
с правками».

**Шаги:**
0. **Назови палитру.** Она не выбрана — их две ([[../craft/design-system]]).
   Палитра выпуска пишется в `run.md`, иначе через месяц не понять, чем
   собран ролик.
1. `tools/doctor.ps1` — проверка стека. Не прошла → чинить, не собирать.
   Главная ловушка: подменённый slim-ffmpeg, тогда падает фильтр `ass`.
2. Разложи сырьё в `projects/<выпуск>/raw/`. **Сырьё не трогается никогда** —
   всё промежуточное пишется в `work/`.
3. `tools/transcribe.py` → `work/words.json` (словные тайминги).
4. Карта катов — по [[vybrat-katy]]. Она **единственный источник правды** по
   таймингу: её читают и сборка, и субтитры ([[../craft/subtitles]]).
5. Параллельно, они друг друга не ждут:
   - звук — по [[obrabotat-zvuk]]: сначала замер, потом обработка;
   - `tools/subs_ass.py --scenes work/clip.mp4` → `work/subs.ass`
     (карты раскладываются по кадрам, не только по времени);
   - обложка — по [[sdelat-oblozhku]];
   - подпись — по [[napisat-post]].
6. `tools/build_reel.py --cuts … --look … --quality …` → `out/<имя>.mp4`.
   Порядок внутри жёсткий: **тонмап → кадрирование → грейд → плашки →
   субтитры → фейд → экспорт** ([[../craft/video-station]]).
   Кадрирование выбирается само: запись экрана не режется, а вписывается.
7. `tools/qa.py --cuts work/cuts.json --rhythm` — **пять** гейтов: кадр,
   звук, громкость, длительность, **ритм**. Длину гейт берёт из карты сам.
   Не прошёл — не показывать, чинить.
8. **Посмотри своими глазами** — [[posmotret-rabotu]], обязательный шаг.
   Контактный лист, три прохода, рубрика из двенадцати вопросов.
   Числа не видят цвет, хук и повтор одинаковых планов.
9. Покажи владельцу ролик, обложку и подпись **вместе с рубрикой**
   (чекпоинт [[../pipelines/streams]]). Правки → пере-гоняй
   **только задетые шаги**.
10. Закрой выпуск по [[zakryt-vypusk]].

**Крайние случаи:**
- Длительность после сборки не сошлась с картой катов → рассинхрон concat,
  тихий брак. Пересобрать, не «подрезать в конце».
- Голос записан через мессенджер → полосу выше 10 кГц не вернуть. Сказать
  владельцу прямо и просить перезапись ([[../craft/audio]]).
